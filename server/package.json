{
  "name": "comtrack-server",
  "version": "1.0.0",
  "description": "ComTrack backend server",
  "main": "dist/server.js",
  "scripts": {
    "start": "node dist/server.js",
    "dev": "nodemon --exec ts-node src/server.ts",
    "build": "tsc",
    "init-db": "node ../database/init-db.js"
  },
  "dependencies": {
    "dotenv": "^16.6.1",
    "pg": "^8.11.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/pg": "^8.10.0",
    "nodemon": "^2.0.22",
    "ts-node": "^10.9.0",
    "typescript": "^5.0.0"
  }
}
